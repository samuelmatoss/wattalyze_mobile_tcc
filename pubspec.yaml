name: wattalyze
description: Um app Flutter para Wattalyze
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_staggered_animations: ^1.1.1
  # HTTP requests
  http: ^1.1.0

  # Charts
  fl_chart: ^0.65.0

  # State management (optional, but recommended)
  provider: ^6.1.1

  # Date formatting
  intl: ^0.19.0

  # SharedPreferences for token storage
  shared_preferences: ^2.2.2

  # Icons
  cupertino_icons: ^1.0.2

  # Google Fonts (NOVA DEPENDÊNCIA para resolver fontes)
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

